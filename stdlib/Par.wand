uses {Clock}

import Clock
import Result

-- Fork-join parallelism, and nothing else.
--
-- Both functions take the most workers to run at once as their first
-- argument, stated rather than inferred: how much a script may do at the
-- same time is a decision about the machine it runs on.
--
-- Workers never outlive the call. There is no handle to a running one, no
-- way to start one outside these two functions, and nothing to await -- so
-- there is no unstructured concurrency to build out of them, and no
-- function needs a different colour for being called from one.
--
-- At the same time; in parallel when the work is pure. Every item starts
-- on a pool of domains, one for each core. At its first effect, the rest
-- of the item runs on the calling domain as a lightweight task, and tasks
-- wait side by side: twenty slow commands overlap.
--
-- A worker is never outside a handler's reach. Every effect runs on the
-- calling domain, inside the handlers the call is inside, so a mock,
-- `--dry-run` and `--trace` see all of them, and a test can never be
-- bypassed by moving work into `Par`.

-- Apply `f` to every element, at most `limit` at a time, and collect the
-- results in the list's own order. An element whose work raises comes back
-- as an `Error` in its place rather than failing the others.
--
-- Every element comes back as a Result, in the order it went in: one
-- branch failing does not take the others with it, and the order is the
-- list's rather than whichever finished first.
--
-- >> Par.map 4 (fn x -> x * 2) [1, 2, 3]
-- [Ok(2), Ok(4), Ok(6)] : List (Result String Int)
--
-- >> Par.map 2 (fn x -> List.head! []) [1]
-- [Error("pattern match failure")] : List (Result String 'a)
let map limit f xs = par_map limit f xs

-- Apply `f` to every element for its effects, at most `limit` at a time.
-- A failure raises once the work is done.
--
-- For what f does rather than what it returns.
--
-- One worker here, so the two lines come out in the list's order. With
-- more, what each branch writes can interleave with what the others write:
-- `Par.map` keeps the *results* in the list's order, and nothing keeps the
-- output in any.
--
-- >> Par.each 1 (fn x -> IO.println "%{x}") [1, 2]
-- 1
-- 2
let each limit f xs = par_each limit f xs

-- Run every thunk at once and answer with the first to finish. The rest
-- are told to stop, and every one of them is joined before this returns.
--
-- First to *finish*, not first to succeed: a winner that raised comes back
-- as `Error`, and a loser that raised is discarded.
--
-- There is no worker limit. The count is the length of the list, and it is
-- already at the call site.
--
-- What a race bounds is when you get the answer, not when the machine goes
-- quiet. A loser blocked in a subprocess finishes that subprocess; wrap it
-- in `Shell.timeout` to bound that too.
--
-- Inside a handler every branch runs, and the handler answers the effects
-- of each.
--
-- The first to finish wins, whether it succeeded or not.
--
-- >> Par.race [fn () -> 1]
-- Ok(1) : Result String Int
let race thunks = par_race thunks

-- Run `thunk` with a deadline. The result is `Error` if the deadline
-- passed first, and the message says how long it waited.
--
-- A deadline on wand code, where `Shell.timeout` is a deadline on a
-- command. It is a race between the work and a sleeper, which is what
-- makes it a wait of a length rather than a wait until an instant -- the
-- difference that keeps it right on a machine whose clock steps.
--
-- The work is asked to stop when the deadline passes, and stops at its
-- next step. Work inside a command finishes that command first: put
-- `Shell.timeout` there as well to bound it.
--
-- A `thunk` that raises comes back as `Error` too, as it does under
-- `race`.
--
-- Refused under a handler for `Clock.sleep`, such as `Test.with_clock`:
-- there the sleeper takes no time, and the deadline would pass before any
-- real work finished. Put that handler inside the thunk instead:
--
-- Par.timeout 200ms (fn () -> Test.with_clock (fn () -> work ()))
--
-- Under any other handler the deadline stands. The wait is real, so give
-- it a length you are willing to wait for.
--
-- >> Par.timeout 5s (fn () -> 1 + 1)
-- Ok(2) : Result String Int
let timeout d thunk = (
  par_deadline_guard ();
  let work = fn () -> Ok (thunk ());
  let sleeper = fn () -> (Clock.sleep d; Error "timed out after %{d}");
  Result.flatten (race [work, sleeper])
)
